Interfacing code with RC car components.

Supports PCA9685 (not currently used) and a Teensy communicating via I2C,
running the code in the `arduino` code directory at the top level of this
repository.
